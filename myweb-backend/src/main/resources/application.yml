spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myweb_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: mins
    password: asd010404
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update     # 엔티티 변경 시 테이블 자동 수정 (개발용)
    show-sql: true         # 실행되는 SQL 콘솔 출력
    properties:
      hibernate:
        format_sql: true   # SQL 들여쓰기 정렬

server:
  port: 8080

app:
  cors:
    allowed-origins: http://localhost:5173  # React 개발 서버 주소
